# Install from the command line (cmd), follow the step:
pip install notebook

Open Jupyter Notebook: jupyter notebook
Open VSCode: code

Version:
pip --version
python --version

# Install from Jupyter Notebook
!pip install pandas
